var triple = function(x) {
   return x * 3;
}

// functions can be assigned to variables

// those variable can be assigned to variables
let waffle = triple;

console.log(triple(4));
console.log(waffle(30));

