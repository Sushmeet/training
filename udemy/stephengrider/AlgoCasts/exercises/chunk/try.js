function chunk(arr, splitNum) {
    let chunkedArr = []
    console.log(arrNew);

    for(i = 0; i < arr.length; i++) {
        let tempArr = []
        for (j = 0; j < splitNum; i++) {
            tempArr[i] = tempArr.push()
        }
    }

}



chunk([1, 2, 3, 4], 2);
