/* Array literal is a list of zero
or more expressions, each of which
reprsents an array element.
*/

// creating an array literal
// array literal is a type of object initializer
// array literals are also called array objects.
let arrayOfCars = ['honda', 'toyota', 'bmw'];

// Boolean Literal vs boolean object.
// The Boolean object is a wrapper
// around the primitive Boolean data type